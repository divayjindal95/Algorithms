#include <iostream>
#include <set>
using namespace std;
int main(){
  set<int> tree;
  for(int i=0;i<5;i++)
    tree.insert(i);
  //for (std::set<int>::iterator it=myset.begin(); it!=myset.end(); ++it)
  //  std::cout << ' ' << *it;
  int a[5];
  cout<<sizeof(a);
}
